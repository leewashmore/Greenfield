<UserControl x:Class="GreenField.Gadgets.Views.ViewPerformanceGrid"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
    xmlns:telerikQuickStart="clr-namespace:Telerik.Windows.Controls.QuickStart;assembly=Telerik.Windows.Controls"
    xmlns:telerikCharting="clr-namespace:Telerik.Windows.Controls.Charting;assembly=Telerik.Windows.Controls.Charting"  
    mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="400">

    <Grid x:Name="LayoutRoot" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="1">
            <Grid x:Name="grdRadGridView" >
                <telerik:RadBusyIndicator x:Name="busyIndicatorGrid" IsIndeterminate="True" IsEnabled="True" DisplayAfter="0">
                    <Border telerikQuickStart:ThemeAwareBackgroundBehavior.IsEnabled="True">
                        <telerik:RadGridView Name="dgPerformanceGrid" AutoGenerateColumns="False"
                                 ItemsSource="{Binding PerformanceGridInfo, Mode=TwoWay}"
                                 CanUserDeleteRows="False" IsReadOnly="True"
                                 CanUserSortColumns="True" ScrollMode="Deferred" 
                                 RowIndicatorVisibility="Collapsed" ShowGroupPanel="False"
                                 telerik:StyleManager.Theme="Metro" Visibility="Visible">
                            <telerik:RadGridView.Columns>
                                <telerik:GridViewDataColumn Header="MTD" DataMemberBinding="{Binding MTD}" />
                                <telerik:GridViewDataColumn Header="QTD" DataMemberBinding="{Binding QTD}" />
                                <telerik:GridViewDataColumn Header="YTD" DataMemberBinding="{Binding YTD}" />
                                <telerik:GridViewDataColumn Header="1 Year" DataMemberBinding="{Binding FIRST_YEAR}"/>
                                <telerik:GridViewDataColumn Header="3 Year" DataMemberBinding="{Binding THIRD_YEAR}" />
                                <telerik:GridViewDataColumn Header="5 Year" DataMemberBinding="{Binding FIFTH_YEAR}"/>
                                <telerik:GridViewDataColumn Header="10 Year" DataMemberBinding="{Binding TENTH_YEAR}" />
                            </telerik:RadGridView.Columns>
                        </telerik:RadGridView>
                    </Border>
                </telerik:RadBusyIndicator>
            </Grid>
        </Grid>

    </Grid>
</UserControl>
